FROM node:22-alpine AS build

WORKDIR /app
COPY package*.json ./
RUN npm ci

ARG VITE_API_BASE_URL=
ENV VITE_API_BASE_URL=$VITE_API_BASE_URL

COPY . .
RUN npm run build

FROM caddy:2.8-alpine

COPY --from=build /app/dist /srv/testing-front
COPY Caddyfile /etc/caddy/Caddyfile
